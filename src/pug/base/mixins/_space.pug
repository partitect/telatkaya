//- Line Mixin
mixin line
	- if (isTemplatemonsterTheme) {
		- if (isProduction) {
			="\n"
		- }
	- } else {
		="\n"
	- }

//- Tab Mixin
mixin tab
	- if (isTemplatemonsterTheme) {
		- if (isProduction) {
			="\t"
		- }
	- } else {
		="\t"
	- }

//- LineTab Mixin
mixin lineTab
	+line
	+tab
